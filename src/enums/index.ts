export enum EGenreType {
  SERIES = "series",
  SINGLE = "single",
}

export enum AUTHENTICATION {
  LOGIN = "login",
  REGISTER = "register",
}

export enum EStoryType {
  NEW = "new",
  HOT = "hot",
  FULL = "full",
}

export enum EThemes {
  DARK = "dark",
  LIGHT = "light",
}
